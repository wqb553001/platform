package com.doctor.assistant.experiment.czy.repository;import com.doctor.assistant.experiment.czy.entity.EvalInputMessage;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.data.jpa.repository.Query;import org.springframework.data.repository.query.Param;import org.springframework.stereotype.Repository;import java.util.List;@Repositorypublic interface EvalInputMessageRepository extends JpaRepository<EvalInputMessage, String> {    List<EvalInputMessage> findAllByMessageNameNotNull();    @Query(nativeQuery=true, value = "SELECT u.* FROM EvalInputMessage u LIMIT ?")    List<EvalInputMessage> findByLimit(int limitNum);    List<EvalInputMessage> findByMessageName(String messageName);    List<EvalInputMessage> findByCreatorId(Integer creatorId);    EvalInputMessage findByMessageId(Integer messageId);    // 根据 消息名称 和 创建人编号 查询    List<EvalInputMessage> findByMessageNameAndAndCreatorId(@Param("messageName")String messageName, @Param("creatorId")Integer creatorId );}