package com.doctor.assistant.experiment.czy.entity;import com.doctor.assistant.experiment.czy.poi.excel.annotation.Excel;import lombok.Data;import javax.persistence.Entity;import javax.persistence.Id;import javax.persistence.Table;import java.io.Serializable;/** * 引接所得的数据记录 * 输入信息 —— 原始数据 */@Entity@Table@Datapublic class EvalInputMessage implements Serializable {//    @NotNull    @Excel(name="消息编号",width=15)    @Id    Integer messageId;          // '消息编号',    String inputFileName;       // '对应的输入文件名',//    @NotNull    @Excel(name="消息名称",width=15)    String messageName;         // '消息名称',//    @NotNull    @Excel(name="创建人工号",width=15)    String creatorId;           // '创建人工号',//    @NotNull    @Excel(name="创建时间",width=15)    String createTime;          // '创建时间',    @Excel(name="最后操作方式",width=15)    String lastOperationMode;   // '最后操作方式 1=add，2=update，3=logic，4=delete',    @Excel(name="最后操作员编号",width=15)    String lastOperatorId;      // '最后操作员编号'    @Excel(name="最后操作时间",width=15)    String lastOperateTime;     // '最后操作时间 yyyy-mm-dd hh:mm:ss',//    @NotNull    @Excel(name = "目标表名称")    String targetTable;         // '目标表名称',    @Excel(name="备注",width=15)    String comment;             // '备注',    @Excel(name="类型",width=15)    Integer type;               // '1=文件，2=MQ，3=数据库视图，4=数据库表',    Integer statue;             // 状态 0=新入库，1=处理中，2=处理完成    public EvalInputMessage() {    }    /**     *     * @param messageId         '消息编号',     * @param inputFileName         '对应的输入文件名',     * @param messageName           '消息名称',     * @param creatorId         '创建人工号',     * @param createTime            '创建时间',     * @param lastOperationMode      '最后操作方式 1=add，2=update，3=logic，4=delete',     * @param lastOperatorId            '最后操作员编号'     * @param lastOperateTime           :ss',     * @param targetTable           '目标表名称',     * @param comment           '备注',     * @param type          '1=文件，2=MQ，3=数据库视图，4=数据库表',     */    public EvalInputMessage(Integer messageId, String inputFileName, String messageName, String creatorId, String createTime, String lastOperationMode, String lastOperatorId, String lastOperateTime, String targetTable, String comment, Integer type, Integer statue) {        this.messageId = messageId;        this.inputFileName = inputFileName;        this.messageName = messageName;        this.creatorId = creatorId;        this.createTime = createTime;        this.lastOperationMode = lastOperationMode;        this.lastOperatorId = lastOperatorId;        this.lastOperateTime = lastOperateTime;        this.targetTable = targetTable;        this.comment = comment;        this.type = type;        this.statue = statue;    }}